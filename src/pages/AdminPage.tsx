/**
 * Administration sur-mesure — accès réservé aux profils `is_admin`.
 * Phase 3 du plan : tableau de bord, CRUD cours & catégories, upload,
 * commandes, élèves, publications.
 */
export function AdminPage() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-24">
      <h1 className="font-display text-3xl font-semibold text-ink">
        Administration
      </h1>
      <p className="mt-4 text-ink-soft">
        Interface de gestion — construite en phase&nbsp;3.
      </p>
    </main>
  )
}
